services:
  app:
    build: .
    volumes:
      - .:/app
    command: tail -f /dev/null
    stdin_open: true
    tty: true
    ports:
      - "8000:8000"
    deploy:
      resources:
        limits:
          memory: 1024M